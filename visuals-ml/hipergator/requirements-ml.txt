# Extra Python deps for the baselines (on top of the pytorch/pytorch base image,
# which already provides torch + torchvision).
#
# NOTE: train.def does NOT read this file. %files is unreliable under --fakeroot
# on HiPerGator, so the container build inlines these same pins as a heredoc in
# its %post section. This file remains for bare (non-container) installs:
#     pip install -r requirements-ml.txt
# Keep the two lists in sync when adding or bumping a dependency.
opencv-contrib-python>=4.8   # TV-L1 optical flow (cv2.optflow) for the temporal stream
scikit-learn>=1.3            # LinearSVR for the introspection SVM stage
scipy>=1.10                  # linear_sum_assignment for MonoDETR's Hungarian matcher
pyyaml>=6.0                  # config loading
pillow>=9.0                  # image IO
numpy>=1.24
psutil>=5.9                  # per-DataLoader-worker RSS in the runner's memory log
                             # (falls back to resource.getrusage without it, which
                             # can't see forked worker processes)
